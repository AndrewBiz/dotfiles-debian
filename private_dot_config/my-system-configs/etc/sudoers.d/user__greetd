#    username hostlist = (userlist) commandlist
#    Поле username указывает, к какому пользователю в системе будет применяться это правило.
#    hostlist сообщает sudo применить это правило к списку системных хостов, которые известны sudo. По умолчанию sudo распознает только локальный компьютер как свой хост.
#    userlist сообщает sudo, какую учетную запись пользователя может заменить поле имени пользователя.
#    Наконец, список команд представляет собой список, разделенный запятыми, который указывает, какие программы в системе может запускать этот пользователь под этим именем.
_greetd ALL=(ALL) NOPASSWD:/sbin/poweroff, /sbin/reboot, /sbin/shutdown
